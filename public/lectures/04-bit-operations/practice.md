# Практика — Побитовые операции

## 1. Маска чтения битов

Считай 4 бита, начиная с позиции `p`:

```js
function readBits(value, p) {
  // TODO: вернуть значение 4 бит
}
```

## 2. Маска записи битов

Запиши 4 бита `x` в позицию `p`:

```js
function writeBits(value, p, x) {
  // TODO: вернуть новое число
}
```

## 3. Проверка флагов

```js
const CAN_READ = 0b0001;
const CAN_WRITE = 0b0010;
const CAN_DELETE = 0b0100;

const role = CAN_READ | CAN_WRITE;
```

- Как проверить `CAN_DELETE`?
- Как добавить право удаления?
- Как убрать право записи?

## 4. Сдвиги

```js
console.log(0b1000 >> 1);
console.log(0b1000 >>> 1);
console.log(-1 >> 1);
console.log(-1 >>> 1);
```

Поясни результат.

## 5. XOR как «переключатель»

Сделай функцию, которая переключает бит на позиции `p`:

```js
function toggleBit(value, p) {
  // TODO
}
```

## 6. Конвертация в 32‑битную область

```js
console.log(2 ** 32 >>> 0); // ?
console.log((2 ** 32 + 5) >>> 0); // ?
```

Поясни, почему так.
